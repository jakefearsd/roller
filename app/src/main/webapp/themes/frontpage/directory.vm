#set($fdPageTitle = "Weblog Directory")
#includeTemplate($model.weblog "_header")

#set($maxResults = 25)

## The portal-era single-weblog profile view (?weblog=<handle>, once rendered
## by the now-deleted _blogprofile.vm) is retired: the approved front-door design
## (docs/design/journal/frontpage-front-door.html) links directory cards straight to the weblogs themselves
## ($blog.absoluteURL, see weblog.vm/_blogdirectory.vm), so there is no
## in-design door back into a per-weblog profile page.
#set($pageLength = $maxResults)
#includeTemplate($model.weblog "_blogdirectory")

#includeTemplate($model.weblog "_footer")
