<!doctype html>
<html lang="$model.weblog.getLanguageTag()">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="Content-Security-Policy" content="default-src 'none'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src * data:; frame-src https://www.youtube-nocookie.com https://player.vimeo.com; base-uri 'self'; connect-src 'self'; form-action 'self'; frame-ancestors 'none'">
    ## $model.weblog.name is pre-escaped by WeblogWrapper#getName
    ## (StringEscapeUtils.escapeHtml4) -- emit it bare, or it double-encodes
    ## ("&amp;" -> "&amp;amp;"), same as frontpage/weblog.vm's $blog.name.
    ## $model.term is already escaped by SearchResultsModel#getTerm
    ## (escapeXml10), so it is emitted bare here exactly like
    ## $model.weblog.name above; escaping it again would double-encode.
    #if($utils.isNotEmpty($model.term))
    <title>Search: $model.term : $model.weblog.name</title>
    #else
    <title>Search results : $model.weblog.name</title>
    #end
    <link href="$url.absoluteSite/favicon.svg" rel="icon" type="image/svg+xml" />
    #showAutodiscoveryLinks($model.weblog)
    #showAnalyticsTrackingCode($model.weblog)
    <link rel="stylesheet" href="$model.weblog.stylesheet">
    #showSeoHead()
    #showGalleryGridStyles()
    #showGalleryAssets()
    #showEmbedAssets()
    #showAudienceAssets()
    #showMapAssets()
</head>
<!-- searchresults -->
<body class="tg">
<a class="tg-skip" href="#main">Skip to content</a>

<header class="tg-header">
    <h1 class="tg-name"><a href="$url.home">$model.weblog.name</a></h1>
    <nav class="tg-nav">
        <ul>
        #showPageLinks($model.weblog)
        </ul>
    </nav>
</header>

<main class="tg-main" id="main">
    #set($pager = $model.getWeblogEntriesPager())
    <div class="tg-search-head">
        #showWeblogSearchAgainForm($model.weblog)
        #if($model.hits > 0)
        <nav class="tg-pager" aria-label="More search results">
            #showNextPrevSearchControl($pager)
        </nav>
        #end
    </div>
    ## SearchResultsModel.getErrorMessage() carries a WebloggerException's
    ## message (Lucene down, a malformed query) and was rendered by nothing,
    ## so a broken index looked exactly like a search that found nothing.
    ## Reported here with a FIXED sentence -- the exception text is internal
    ## detail on a public page, for the same reason the Velocity error
    ## templates may not print it. Checked before the zero-hit branch,
    ## because a failed search also reports zero hits.
    #if($utils.isNotEmpty($model.errorMessage))
    <p class="tg-search-error">Search is temporarily unavailable. Please try again shortly.</p>
    #elseif($model.hits == 0)
    <p class="tg-search-empty">No entries matched that search. Try a different word, or a shorter phrase.</p>
    #end
    <div class="tg-cards">
        #showWeblogEntriesPager($pager)
    </div>
</main>

</body>
</html>
