<!doctype html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="Content-Security-Policy" content="default-src 'none'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src * data:; frame-src https://www.youtube-nocookie.com https://player.vimeo.com; base-uri 'self'; connect-src 'self'; form-action 'self'; frame-ancestors 'none'">
    <title>#showEntryTitle($model.weblogEntry) : $utils.escapeHTML($model.weblog.name)</title>
    #showAutodiscoveryLinks($model.weblog)
    #showAnalyticsTrackingCode($model.weblog)
    <link rel="stylesheet" href="$model.weblog.stylesheet">
    #showSeoHead()
    #showGalleryGridStyles()
    #showGalleryAssets()
    #showEmbedAssets()
    #showAudienceAssets()
    #showMapAssets()
</head>
<!-- permalink -->
<body class="tg">

<header class="tg-header">
    <p class="tg-name"><a href="$url.home">$utils.escapeHTML($model.weblog.name)</a></p>
    <nav class="tg-nav">
        #showWeblogCategoryLinksList()
        <ul>
        #showPageLinks($model.weblog)
        </ul>
    </nav>
</header>

<main class="tg-main tg-main-entry">
    #set($pager = $model.getWeblogEntriesPager())
    #showWeblogEntriesPager($pager)

    <nav class="tg-pager">
        #showNextPrevEntriesControl($pager)
    </nav>

    <section class="tg-comments">
        #showWeblogEntryComments($model.weblogEntry)
        #showWeblogEntryCommentForm($model.weblogEntry)
    </section>
</main>

<footer class="tg-footer">
    <form class="tg-search" method="get" action="$url.search">
        <input type="search" name="q" placeholder="Search guides" aria-label="Search">
    </form>
    #showSubscribeForm($model.weblog "Get new guides by email")
</footer>

</body>
</html>
