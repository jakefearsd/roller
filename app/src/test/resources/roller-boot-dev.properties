# used during mvn spring-boot:run (./roller dev), not Roller testing (mvn test)

installation.type=auto

database.configurationType=jdbc
database.jdbc.driverClass=org.postgresql.Driver
# PostgreSQL from docker-compose.yml (docker compose up -d postgres)
database.jdbc.connectionURL=jdbc:postgresql://localhost:5433/rollerdb
database.jdbc.username=roller
database.jdbc.password=roller


#see http://wiki.eclipse.org/EclipseLink/Examples/JPA/Logging
#eclipselink.logging.file=target/eclipselink.log
# OFF, SEVERE, WARNING, INFO, CONFIG, FINE, FINER, ALL
#eclipselink.logging.level=FINER

# use properties-based mail (no JNDI mail session in embedded Tomcat dev mode)
mail.configurationType=properties
mail.hostname=localhost


# use src copy of themes for read-only access. This file is filtered by the
# maven-resources-plugin during process-test-resources, whose basedir token
# below resolves to the app/ module directory (not the repo root).
themes.dir=${basedir}/src/main/webapp/themes

# put work in work dir
search.index.dir          =${project.build.directory}/work/search-index
mediafiles.storage.dir    =${project.build.directory}/work/mediafiles

# turn on theme reloading
themes.reload.mode=true

# turn off caching for testing/debugging
cache.sitewide.enabled=false
cache.weblogpage.enabled=false
cache.weblogfeed.enabled=false
